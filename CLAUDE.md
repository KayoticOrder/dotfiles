# Code comments

Only write a comment when it's actually useful — explaining a non-obvious
"why" (a constraint, a workaround, a gotcha that would bite someone editing
the code without it). Don't comment what the code already says.

Keep it as short as it can be while still being useful. One line beats a
paragraph; no comment beats a one-liner that just restates the code.
